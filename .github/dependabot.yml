version: 2
updates:
  # Workflow actions are pinned to commit SHAs so a repointed upstream tag
  # cannot change what runs. Dependabot bumps those SHAs in reviewable PRs
  # so the pins stay current instead of rotting on an old release.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
